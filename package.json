{
  "name": "automatizacion-correos-root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BE,FE -c cyan,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "nodemailer": "^6.10.1"
  }
}
